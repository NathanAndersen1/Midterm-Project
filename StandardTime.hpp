#include <string>

class StandardTime {
    // attributes
    protected:
        std::string name;  
        std::string gender;
        int raceLength;
        int raceTime;
        int age;  

    public:
        
};