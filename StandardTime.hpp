#include <string>
#include <vector>

class StandardTime {
    protected:
        std::string name; 
        std::string gender; 
        int raceLength;
        double rawTime;
        int age; 
        std::vector<double> lapTimes;
    public:
        StandardTime() {

        }
        
};